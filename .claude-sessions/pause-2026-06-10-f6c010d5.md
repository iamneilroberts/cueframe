# Auto Session Pause
**Date:** 2026-06-10 at 19:17

**Session:** f6c010d5
**Branch:** (not a git repo)
**Uncommitted changes:** no

## Git State
```
(unavailable)
```

## Recent Changes
```
(unavailable)
```

## Recent Commits
```
(unavailable)
```

## Last Conversation Context
**Human:** <system-reminder>
The user named this session "cueframe". This may indicate the session's focus or intent.
</system-reminder>

**Assistant:** The hook is gating code reads. Let me pull the conceptual kernel — the capture→spec→callout→player pipeline — from docs and the reel/capture scripts, which is what cueframe would generalize.

**Assistant:** I've got the shape of it. Here's what I understand before I ask the few decisions that actually change what I write:

**The concept** — Cueframe generalizes the "reel" kernel buried inside voygent-demo (`capture` scripts → `spec.json` of `frames[]` → `callouts[]` → animated player like `ReelCallout`/`timeline.ts`), stripped of all voygent-specifics. Three acts:
1. **Capture/Discover** — point Claude Code + browser tools (chrome-devtools MCP) at a repo or running SPA, let it learn the app, then t...

**Assistant:** Locked in: **clean-room rebuild**, ship as **both a Claude Code plugin and an `npx` CLI**, export **HTML + MP4 + GIF**, and "done" = **MVP + OSS hygiene, publishable**. (On the cutoff: I'll treat your pasted `cueframe-callouts` skill as the reference example and have the build produce *both* a capture skill and a callout skill — if you had a second example to paste, drop it in and I'll fold it.)

Here's the design of the document before I write it. It's a self-contained **build brief** — `GOAL.m...

**Assistant:** Both notes are load-bearing and I'm baking them in:
- **Capture quality is a hard contract**, not best-effort: `caption` + `axDigest` + `boxes` are the fields semantic resolution and auto-anchoring stand on. The brief will make rich per-frame metadata a validated acceptance criterion, not a nice-to-have — if the Showrunner skimps, the whole callout experience collapses to frame-number editing.
- **Tone is configurable via `meta.voice`**, not hardcoded. The plain-sentence / no-em-dash rule become...

**Assistant:** Done. `GOAL.md` is written at the repo root — `/home/neil/dev/cueframe/GOAL.md`.

It's a self-contained build brief with a hard **Definition of Done** (§6) so `/goal` knows when to stop. Structure:

- **§1 Vision** + **§2 the three acts** (Capture/Showrunner → Author → Play & Export)
- **§3 the `spec.json` contract** — concrete `FrameRecord` / `Callout` schemas
- **§4 distribution** — CC plugin *and* `npx cueframe` CLI from one core
- **§5 deliverables D1–D10** · **§6 the stop gate** (7 checkabl...

## Instructions
Continue the work from this session. Review the git state and conversation
context above to understand where things left off. Ask the user what to
focus on next if the direction isn't clear from context.
