---
description: Record a workflow on a running web app into a Cueframe spec.json (the Showrunner).
---

Use the **cueframe-capture** skill to record a demo.

The user's workflow to capture: $ARGUMENTS

Follow the skill: learn the app (URL + stable selectors), turn the workflow into a capture
scenario, run `npx cueframe capture`, then validate the resulting spec.json has zero schema
errors and zero capture defects before handing off.
